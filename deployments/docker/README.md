# Docker Deployments

Docker Compose stuff for running everything locally.

## What Goes Here

- `docker-compose.yml` (will be in the root)
- Nginx config (in the nginx folder)

Still figuring out the exact setup, but this is where the Docker configuration will live.
