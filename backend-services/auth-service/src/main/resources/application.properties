spring.application.name=com.battlearena.auth

# MongoDB Configuration
spring.data.mongodb.host=battle-arena-mongodb
spring.data.mongodb.port=27017
spring.data.mongodb.database=battlearena

# Server Configuration
server.port=8081
server.address=0.0.0.0

# Spring Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin
spring.security.user.roles=ADMIN

# CORS Configuration
# spring.web.cors.allowed-origins=http://localhost:4200
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allowed-headers=Authorization,Content-Type,X-Requested-With,Accept,Origin,Access-Control-Request-Method,Access-Control-Request-Headers
# spring.web.cors.exposed-headers=Access-Control-Allow-Origin,Access-Control-Allow-Credentials
# spring.web.cors.allow-credentials=true
# spring.web.cors.max-age=3600

# Allow public access to /api/auth/test
spring.security.filter.order=0

# JWT configs for Step 3
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}
