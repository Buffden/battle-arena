{
  "name": "game-engine",
  "version": "1.0.0",
  "description": "Game engine service for Battle Arena - handles game physics, turns, scoring, and real-time game state",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo \"Build step - add build commands here if needed\"",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "echo \"Linting not yet configured - add ESLint configuration\""
  },
  "keywords": [
    "game-engine",
    "physics",
    "websocket",
    "socket.io"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
